---
interface Props {
	date: string;
}

const { date } = Astro.props;
---

<time datetime={new Date(date).toISOString()}>
	{
		new Date(date).toLocaleDateString('es-ve', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
